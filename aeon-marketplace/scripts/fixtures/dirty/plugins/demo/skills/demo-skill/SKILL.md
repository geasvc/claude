---
name: demo-skill
description: Planted D8 and D12b — the frontmatter carries OKF fields it must not, and the body has Thai prose outside code and quotes.
type: Skill
id: SKL-demo
owner: พี่ปู
---

# demo-skill

**Planted D8:** `type`, `id` and `owner` are OKF fields. `SKILL.md` frontmatter is a contract with
Claude Code and holds `name` and `description` only (DOC-STANDARD §4).

**Planted D12b:** บรรทัดนี้เป็นร้อยแก้วภาษาไทยที่อยู่นอกบล็อกโค้ดและนอกเครื่องหมายคำพูด
ซึ่งเป็นสิ่งที่ D12b มีไว้ชี้ให้คนดู ไม่ใช่ตัดสินแทน

Quoted Thai like "ทำเสร็จแล้ว 🛑" is NOT counted — that is a literal the command must print.
