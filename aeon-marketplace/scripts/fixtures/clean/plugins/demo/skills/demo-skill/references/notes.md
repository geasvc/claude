# notes — what `/demo:thing` does

Does the one thing, writes it down, stops. No frontmatter here on purpose: Claude reads reference
files mid-task, so eight lines of frontmatter per file per session buys nothing (DOC-STANDARD §4).

Thai belongs in quoted user-facing strings only, like the confirmation line:
"ทำเสร็จแล้ว 🛑 รออนุมัติ" — a literal the command must print verbatim, never translated.
