# CLAUDE.md — fixture marketplace `dirty`

A marketplace that breaks every check in `scripts/verify-design.mjs` on purpose. Each violation is
planted once and traceable to a file, so a count that moves can always be walked back to a cause.

**This file plants D10:** no owner, no review date. Do not "fix" it.
